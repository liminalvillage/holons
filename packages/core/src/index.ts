// @holons/core — shared domain logic.
// Subpath imports preferred: import { calculateUserScore } from '@holons/core/scoring'
// This barrel intentionally re-exports nothing by default to avoid coupling all
// consumers to all domains. Workers populate it via subpath modules in src/<domain>/.
export {};
