{
  "title": "previous_rituals",
  "description": "Records stored at (holonId, 'previous_rituals', <ritualId>). One record per completed ritual. (Some writers also persist a collection array at the holon's own key \u2014 strict mode would require a separate shape for that wrapper; this schema covers the per-ritual record.)",
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    },
    "title": {
      "type": "string"
    },
    "date": {
      "type": "string",
      "format": "date-time"
    },
    "artifact": {
      "type": "object",
      "description": "Ritual artifact: { format, text, quotes, ascii_glyph }."
    },
    "design_streams": {
      "type": "array"
    }
  },
  "required": [
    "id",
    "title",
    "date"
  ]
}
