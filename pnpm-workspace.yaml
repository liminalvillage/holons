packages:
  - "apps/*"
  - "packages/*"
  # holosphere is consumed from npm (see pnpm.overrides), not as a workspace
  # package — exclude the vendored copy so it isn't linked or built locally.
  - "!packages/holosphere"
