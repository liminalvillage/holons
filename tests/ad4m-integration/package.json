{
  "name": "@holons/ad4m-integration-tests",
  "version": "1.0.0",
  "type": "module",
  "description": "Integration tests for Harvest AD4M adapter using a real AD4M executor",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:crud": "vitest run holons-crud --reporter=verbose",
    "test:multi-user": "vitest run holons-multi-user --reporter=verbose",
    "test:sync": "vitest run holons-sync --reporter=verbose",
    "test:schema-bridge": "vitest run holons-schema-bridge --reporter=verbose"
  },
  "devDependencies": {
    "@coasys/ad4m": "^0.11.1",
    "@types/ws": "^8.5.5",
    "graphql": "^16.6.0",
    "graphql-ws": "^5.12.1",
    "typescript": "^5.3.0",
    "vitest": "^1.6.0",
    "ws": "^8.13.0"
  },
  "prerequisites": [
    "ad4m-executor binary must be built: cd ~/Desktop/ad4m && cargo build --release -p ad4m-executor",
    "kitsune2-bootstrap-srv must be available on PATH (for multi-user tests with local HC services)"
  ],
  "dependencies": {
    "@apollo/client": "3.7.10"
  },
  "overrides": {
    "@apollo/client": "3.7.10"
  }
}
