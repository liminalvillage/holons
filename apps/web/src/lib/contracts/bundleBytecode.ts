// SPDX-License-Identifier: AGPL-3.0-or-later
// The Bundle bytecode lives in core so every surface deploys the same
// contract; this module only keeps the dashboard's import path alive.
export { BUNDLE_BYTECODE } from "@holons/core/flows";
